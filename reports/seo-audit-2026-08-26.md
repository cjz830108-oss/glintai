# Glint AI 站点审计 + 修复报告（2026-08-26）

## 一、审计方法与范围
- 本地仓库全量扫描：29 篇博客 + 16 工具页的 canonical、JSON-LD、内链、sitemap 一致性、重复 FAQ、首页 16 工具文案一致性。
- 线上抽样验证：首页、新博客 `free-grammar-checker-no-signup`、工具页 `grammar-checker` 实际渲染。

## 二、技术底座（健康 ✅）
- 全部 29 博客 + 16 工具页 canonical 均以 `.html` 结尾。
- 全部页面含 2 个 JSON-LD 块（Article + FAQPage）。
- sitemap 46 个 URL 与实际文件一一对应，无 404 死链。
- 首页 "16 free tools / all 16" 文案一致，无 9-vs-16 矛盾。
- 本地检测无重复 FAQ（`Frequently asked questions` 每页仅 1 个 h2）。

## 三、发现的问题与处理

### 问题 1：线上重复 FAQ（真 bug，已本地修复待上线）
- 现象：WebFetch 线上 `free-grammar-checker-no-signup` 出现两个 FAQ 区块。
- 根因：本地 commit `cc99163` 已删除正文内嵌 FAQ、仅保留 faqs 数组渲染，但 **该 commit 尚未 push**，线上仍是旧版本。
- 状态：本地已修复，待 push 上线。

### 问题 2：工具页内链不规范 `/index.html#`（已本地修复）
- 现象：16 个工具页共 96 处链接写作 `/index.html#tools` 等，会制造与首页 `/` 等价的第二个 URL，分散权重。
- 修复：全量改为 `/#tools` 规范锚点（commit `82ec5df`）。

### 问题 3：Supabase 免费版自动暂停（影响登录/付费，已加防护）
- 现象：官方邮件通知项目已暂停（7 天无数据库活动触发），登录与 PayPal webhook 写 plan 暂失效。静态内容与工具不受影响。
- 修复：新增 `.github/workflows/keepalive.yml`，每 6 小时经 Vercel 代理 ping 一次数据库，产生真实活动，**永久避免自动暂停**（零成本、无需升 Pro）。
- 待办：当前仍需大哥去 Supabase 后台点一次 **Resume** 把它救活（心跳只在激活后才持续保活）。

### 问题 4：代理 fallback ref 拼写 typo（已修正）
- 现象：`api/supabase-proxy.js` 写死 fallback `czqupmfabeliihtligdy`（多一个 i），真实 ref 为 `czqupmfabelihtligdy`。
- 影响：无（Vercel 环境变量 `SUPABASE_URL` 已覆盖），但已顺手修正保持代码正确。

### 误报澄清
审计脚本初报 128 条"问题"绝大多数为误报：`/icon.svg`、`/assets/tools.css` 是合法资源链接；经规则修正后确认无真实 [CANON]/[JSONLD]/[SITEMAP]/[FAQDUP] 缺陷。

## 四、已提交但未 push 的 commit（需新 PAT 上线）
- `cc99163` fix(blog): 删除 5 篇新博客重复 FAQ 段
- `82ec5df` fix: 工具页锚点规范化 + Supabase 心跳 workflow + 代理 typo 修正

## 五、下一步
- SEO 下一批选题由关键词研究员后台规划中（comparison/alternative 类高商业意图词优先）。
- 收到短期 fine-grained PAT 后，一次性 push `cc99163`+`82ec5df` 触发 Vercel 部署，修复全部线上问题并启用心跳。
